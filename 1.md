when usdc found can we please also do one more interal call then return value to frontend 

when usdc found we need to push data to db table 

table : everywherepaytransactions 

rows : txhash, amount, chain, tokenid (alway pass usdc address from same chain where tx was found) , walletid , sender(from who), status 


if table and row doesnt exits then create them 
and please note before pusing to table first check if hash already exits in 

if hash exits then it ingore it (na response)

